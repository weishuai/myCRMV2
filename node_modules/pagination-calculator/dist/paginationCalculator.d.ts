export interface PageCalculatorOptions {
    total: number;
    current?: number;
    pageSize?: number;
    pageLimit?: number;
}
export declare type Pages = (number | "...")[];
export interface PageInformation {
    total: number;
    current: number;
    pageCount: number;
    pages: Pages;
    next: number | false;
    previous: number | false;
    showingStart: number;
    showingEnd: number;
}
export declare const paginationCalculator: (options: PageCalculatorOptions) => PageInformation;
